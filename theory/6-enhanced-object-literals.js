const name = 'Javascript';
const price = 1000;

const course = {
  name: 'jihi',
  price,
  getName() {
    return name;
  },
};
console.log(course.name);
console.log(course.getName());

let fieldName = 'name';
let fieldValue = 'price';
